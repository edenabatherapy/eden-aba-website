import { INSURANCE_COVERAGE_LOGOS, type InsuranceCoverageLogo } from "@/lib/insurance-coverage-logos";

export type HomeInsuranceLogo = InsuranceCoverageLogo;

/** Homepage insurance toolbox — same canonical payer list as the coverage page. */
export const HOME_INSURANCE_LOGOS: HomeInsuranceLogo[] = INSURANCE_COVERAGE_LOGOS;
