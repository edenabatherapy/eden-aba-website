import EdenNewsletter from "@/components/common/EdenNewsletter";

/** @deprecated Labels ignored — uses sitewide EdenNewsletter (Home page design). */
export default function AdmissionsNewsletterSection() {
  return <EdenNewsletter source="admissions-newsletter" />;
}
